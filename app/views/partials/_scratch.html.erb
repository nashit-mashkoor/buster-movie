                <% if (is_admin? || (current_user == review.user)) %>    
                  <br />  
                  <div class = "row" align="center">
                    <div class= "col-md-4 col-md-offset-2">                   
                      <button class="my-edit-btn movie-item-style-1 movie-item-style-2 "><%= link_to ' Edit ', edit_review_path(review) %></button>
                    </div>
                    <div class= "col-md-4 ">                   
                      <button class="my-submit-btn movie-item-style-1 movie-item-style-2 "><%= link_to ' Movie ', movie_path(review.movie) %></button>
                    </div>
                    <div class= "col-md-4"> 
                      <button class="my-delete-btn movie-item-style-1 movie-item-style-2 "> <%= link_to 'Delete', review_path(review), method: :delete, data: { confirm: 'Are you sure?' } %></button>
                    </div>
                  </div>
                <% end %>


              <input class="submit" type="submit" placeholder="submit">

