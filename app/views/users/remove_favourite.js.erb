$(document).ready(function(){
   flash_update("<%= j (render partial: 'layouts/jsflash') %> ");
      $("div#user-favourite-btn").empty()
      $("div#favourite-list-<%= @movie.id %>").remove()
      $("div#user-favourite-btn").html(" <%= j(render partial: 'users/add_button', locals: {movie: @movie})%>");
});
